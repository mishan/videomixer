# Development-only dependencies.
#
# Kept out of requirements.txt because that file is what the Dockerfile
# installs -- a linter has no business in the runtime image.
#
#   pip install -r requirements-dev.txt
-r requirements.txt

flake8>=7,<8
