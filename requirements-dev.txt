# Development-only dependencies.
#
# Kept out of requirements.txt because that file is what the Dockerfile
# installs -- linters and test frameworks have no business in the runtime
# image.
#
#   pip install -r requirements-dev.txt
-r requirements.txt

flake8>=7,<8
pytest>=8
pytest-asyncio>=0.24
